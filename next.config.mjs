/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['encrypted-tbn0.gstatic.com','mir-s3-cdn-cf.behance.net','static.vecteezy.com'],
      },
    
};

export default nextConfig;
